<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="./css/style.css">    
  {{!-- This likely won't be needed --}}
  <script src="https://cdn.tailwindcss.com?plugins=typography,line-clamp"></script>
</head>

<body>
  <div class="flex-col justify-around mx-0">
    <header class="flex-auto justify-between content-center p-2 mx-0">
        <h1 class="text-4xl w-full sm:w-32 md:w-48 lg:w-64 xl:w-96">REAL RECIPES</h1>
        <nav class="flex items-center justify-between bg-lime-300">
            <ul class="grid grid-cols-5 p-4">
                <li class="p-2 text-center"><a href="/">Home</a></li>
                <li class="p-2 text-center"><a href="/about">About</a></li>
                {{#if logged_in}}
                <button id="logout">Logout</button>
                {{else}}
                <li class="p-2 text-center"><a href="/login">Login/Signup</a></li>
                {{/if}}
                <li class="p-2 text-center"><a href="/contribute">Contribute</a></li>
                <form class="flex-row">
                    <input type="text" id="search-input" placeholder="Search">
                </form>
            </ul>
        </nav>
    </header>
    <main class="container mx-auto mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="./js/logout.js"></script>
  {{/if}}
</body>

</html>
